export const metadata = {
  title: "Вход",
  description: "Вход в личный кабинет Ozelim.",
  robots: { index: false, follow: false },
};

export default function LoginLayout({ children }) {
  return children;
}
