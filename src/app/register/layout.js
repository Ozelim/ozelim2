export const metadata = {
  title: "Регистрация",
  description: "Регистрация нового аккаунта Ozelim.",
  robots: { index: false, follow: false },
};

export default function RegisterLayout({ children }) {
  return children;
}
